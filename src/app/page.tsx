import Inicio from "./Inicio";

//A landing traz o próprio cabeçalho: a navegação da área autenticada (AppShell)
//só existe dentro de /dashboard e /profile.
export default function Home() {
  return <Inicio />;
}
